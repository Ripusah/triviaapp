<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.triviaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TriviaApp">
        <activity android:name=".ui.HistoryAtivity"/>
        <activity android:name=".ui.SummaryActivity" />
        <activity android:name=".ui.Question3Activity" />
        <activity android:name=".ui.Question2Activity" />
        <activity android:name=".ui.Question1Activity" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Theme.TriviaApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>